[package]
name = "myhttp-bin"
version = "0.1.0"
edition.workspace = true
publish = false

[dependencies]
clap = { workspace = true }
enventory = { workspace = true, features = ["clap"] }
myhttp = { path = "../myhttp" }
